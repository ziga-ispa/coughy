import SwiftUI

struct ProfileView: View {
    var body: some View {
        NavigationView {
            Text("Profile coming soon")
                .foregroundStyle(.secondary)
                .navigationTitle("Profile")
        }
    }
}
